{
  "name": "taskflow-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port 8080"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}